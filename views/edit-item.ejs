<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/edit-item.css">
</head>
<body>
    <%- include('partials/header') %>

    <div class="form-container">
        <h1>Edit Task</h1>
        <form action="/items/<%= item.id %>/edit" method="POST">
            <label for="content">Task Content:</label>
            <input type="text" id="content" name="content" value="<%= item.content %>" required>
            <input type="hidden" name="memberId" value="<%= item.member_id %>">
            <button type="submit" class="submit-button">Update Task</button>
        </form>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
